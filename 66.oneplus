class Solution(object):
    def plusOne(self, digits):
        s=0
        for i in digits:
            s=(s*10)+i
        s=s+1
        res=[]

        while s>0:
            res.append(s%10)
            s//=10
        res.reverse()
        return res
